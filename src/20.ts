class SchoolProject {
  constructor() {
    this.name = "Basic GitHub Repository";
    this.description = "A simple school project made with TypeScript.";
  }

  // Example method
  addLesson() {
    console.log("Adding a new lesson.");
  }
}

// Create an instance of the SchoolProject class
const schoolProject = new SchoolProject();

// Call the method to add a lesson
schoolProject.addLesson();
