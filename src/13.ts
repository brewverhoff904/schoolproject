// This example code is for educational purposes only. Do not use it for real projects.
// It's part of the TypeScript documentation and is not intended to be used in production environments.
// For safe usage with TypeScript, you should always check the version at runtime.

function greet(name: string): void {
    console.log(`Hello, ${name}!`);
}

greet("Alice");
